version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./prediction.db:/app/prediction.db
    environment:
      - PYTHONUNBUFFERED=1

    